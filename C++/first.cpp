#include <iostream> // io-> input output, stream --> flow source

using namespace std; // it is a directive which tells the compiler to consider all the symbols in the std namespace as if they are declared in the global namespace.

int main()
{
    cout << "Hello World";// used to print 

    return 0;
}
