#include <iostream>

using namespace std;

int main()
{

    int num1, num2;
    cout << "Enter the first number: ";
    cin >> num1;
    cout << "Enter the second number: ";
    cin >> num2;

    // cin (see in ) is used to take input and cout(see out) is used to print anything.
    // also note that the relational operator is different in both.

    cout << "After Adding: " << num1 + num2;

    return 0;
}
